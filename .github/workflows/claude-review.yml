name: claude-review-paused

# Paused intentionally. Codex remains the required AI review; Kimi is advisory-only.
# Keep this manual no-op workflow so the paused state is explicit and reversible.

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Claude review is paused by repository policy."
