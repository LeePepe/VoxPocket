name: codex-review-legacy-paused

# Legacy pull_request control plane. The required gate is codex-review-target.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Codex review workflow is paused."
